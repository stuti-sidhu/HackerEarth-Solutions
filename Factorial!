#include<stdio.h>
int fact(int n);
void main()
{
    long int factorial;
    int num;
    scanf("%d",&num);
    factorial=fact(num);
    printf("%d",factorial);
}
int fact(int n)
{
    if(n==1)
        return 1;
    return (n*fact(n-1));
}
