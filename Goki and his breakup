#include<stdio.h>

int main()
{
    int n,x,y;
    
    scanf("%d %d",&n,&x);
    
    while(n--)
    {
        scanf("%d",&y);
        
        if(y>=x)
            printf("YES\n");
        else
            printf("NO\n");
    }
    
    return 0;
}
